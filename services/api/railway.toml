[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "true && /app/.venv/bin/uvicorn flow.interfaces.http.main:app --host 0.0.0.0 --port $PORT 2>&1"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
